# MongoDB Connection String
MONGODB_URI=your_mongodb_connection_string

# NextAuth Configuration
NEXTAUTH_SECRET=your_nextauth_secret_key  # Generate with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000

# Google OAuth Credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# JWT Secret (for custom JWT handling, if needed beyond NextAuth's default)
JWT_SECRET=your_jwt_secret_key # Generate with: openssl rand -base64 32

# ImgBB API Key (for image uploads - get free key from imgbb.com/api)
IMGBB_API_KEY=your_imgbb_api_key
